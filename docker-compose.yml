services:
  fire-optimizer:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - .:/app  # <--- THIS IS CRITICAL. Maps your current folder to /app in Docker
      - ./data:/app/data
      - ./results:/app/results
    environment:
      - OSMNX_USE_CACHE=True
    restart: unless-stopped